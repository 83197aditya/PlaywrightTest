{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "Playwright testing example",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "@playwright/test": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.3.0"
  }
}
